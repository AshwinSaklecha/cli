# charge-api

BTW fixture — payments toy app. Hold compiles and checks against this.

This folder is a tiny mock payments service, not a product website. The Hold build pass adds `charge()`, callers, retries, and tests.
